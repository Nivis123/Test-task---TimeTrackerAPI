<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ru.prod.tracker.mapper.TimeRecordMapper">

    <insert id="insert" parameterType="ru.prod.tracker.model.TimeRecord"
        useGeneratedKeys="true" keyProperty="id">
        INSERT INTO time_records (employee_id, task_id, start_time, end_time, description_work)
        VALUES (#{employeeId}, #{taskId}, #{startTime}, #{endTime}, #{descriptionWork})
    </insert>

    <select id="findByEmployeeIdAndPeriod" resultType="ru.prod.tracker.model.TimeRecord">
        SELECT id, employee_id, task_id, start_time, end_time, description_work
        FROM time_records
        WHERE employee_id = #{employeeId}
        AND start_time >= #{start}
        AND end_time &lt;= #{end}
        ORDER BY start_time
    </select>
</mapper>